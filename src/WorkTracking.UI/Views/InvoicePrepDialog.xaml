<Window x:Class="WorkTracking.UI.Views.InvoicePrepDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WorkTracking.UI.ViewModels"
        mc:Ignorable="d"
        Title="Prepare Invoice"
        Width="480" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        d:DataContext="{d:DesignInstance Type=vm:InvoicePrepViewModel}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <StackPanel Margin="20">

        <!-- Summary -->
        <TextBlock Text="Invoice Summary" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,12"/>

        <Grid Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Total hours"/>
            <TextBlock Grid.Column="1" Text="{Binding TotalHours, StringFormat='{}{0:N2} h'}" FontWeight="SemiBold"/>
        </Grid>

        <Grid Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Total amount"/>
            <TextBlock Grid.Column="1" Text="{Binding TotalAmount, StringFormat='{}{0:C}'}" FontWeight="SemiBold"/>
        </Grid>

        <!-- Cap warning -->
        <Border Visibility="{Binding IsOverCap, Converter={StaticResource BoolToVis}}"
                Background="#FFF3CD" BorderBrush="#FFC107" BorderThickness="1"
                CornerRadius="3" Padding="8,5" Margin="0,4,0,4">
            <TextBlock Text="⚠ This amount exceeds the client's invoice cap." Foreground="#856404" FontSize="11"/>
        </Border>

        <!-- Category breakdown -->
        <ItemsControl ItemsSource="{Binding LinesByCategory}" Margin="0,8,0,0">
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:InvoicePrepLine}">
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="80"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding CategoryName}" Foreground="Gray" FontSize="11"/>
                        <TextBlock Grid.Column="1" Text="{Binding Hours, StringFormat='{}{0:N2} h'}" FontSize="11" HorizontalAlignment="Right"/>
                        <TextBlock Grid.Column="2" Text="{Binding Amount, StringFormat='{}{0:C}'}" FontSize="11" HorizontalAlignment="Right"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Separator Margin="0,12,0,12"/>

        <!-- Invoice details -->
        <TextBlock Text="Invoice Details" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,12"/>

        <Grid Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Invoice number *" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1"
                     Text="{Binding InvoiceNumber, UpdateSourceTrigger=PropertyChanged}"
                     Padding="4,3"/>
        </Grid>

        <Grid Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Invoice date" VerticalAlignment="Center"/>
            <DatePicker Grid.Column="1"
                        SelectedDate="{Binding InvoiceDate}"/>
        </Grid>

        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="PDF file (optional)" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1"
                     Text="{Binding PdfPath}"
                     IsReadOnly="True"
                     Padding="4,3"
                     Margin="0,0,4,0"/>
            <Button Grid.Column="2" Content="Browse…"
                    Padding="6,3"
                    Click="BrowsePdf_Click"/>
        </Grid>

        <!-- Action buttons -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Command="{Binding CancelCommand}"
                    Padding="16,6" Margin="0,0,8,0"/>
            <Button Content="Create Invoice"
                    Command="{Binding ConfirmCommand}"
                    Padding="16,6"
                    IsDefault="True"/>
        </StackPanel>

    </StackPanel>
</Window>
