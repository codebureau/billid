<Window x:Class="WorkTracking.UI.Views.AddClientDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WorkTracking.UI.ViewModels"
        mc:Ignorable="d"
        Title="Add Client" Width="380" SizeToContent="Height"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        d:DataContext="{d:DesignInstance Type=vm:AddClientViewModel}">

    <StackPanel Margin="20">

        <TextBlock Text="New Client" Style="{StaticResource SectionHeader}" Margin="0,0,0,16"/>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Name *" VerticalAlignment="Center" Margin="0,0,0,10"/>
            <TextBox   Grid.Row="0" Grid.Column="1"
                       Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                       Margin="0,0,0,10"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Hourly rate *" VerticalAlignment="Center"/>
            <TextBox   Grid.Row="1" Grid.Column="1"
                       Text="{Binding HourlyRate, UpdateSourceTrigger=PropertyChanged, StringFormat='{}{0:F2}'}"
                       Margin="0,0,0,0"/>
        </Grid>

        <TextBlock Text="You can fill in all other details on the Settings tab after saving."
                   Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" TextWrapping="Wrap" Margin="0,12,0,0"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Command="{Binding CancelCommand}" Width="80" IsCancel="True"
                    Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"/>
            <Button Content="Save" Command="{Binding ConfirmCommand}" Width="80"
                    IsDefault="True" Style="{StaticResource PrimaryButton}"/>
        </StackPanel>
    </StackPanel>
</Window>
