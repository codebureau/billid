<UserControl x:Class="WorkTracking.UI.Views.InvoicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:WorkTracking.UI.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:InvoicesViewModel}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320" MinWidth="200"/>
            <ColumnDefinition Width="4"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Invoice list -->
        <DockPanel Grid.Column="0">
            <Border DockPanel.Dock="Top" Style="{StaticResource ToolbarPanel}">
                <TextBlock Text="Invoices" Style="{StaticResource SectionHeader}"/>
            </Border>

            <TextBlock DockPanel.Dock="Top"
                       Text="No invoices yet."
                       HorizontalAlignment="Center" Margin="0,20"
                       Foreground="{StaticResource TextSecondaryBrush}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasInvoices}" Value="False">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <ListBox ItemsSource="{Binding Invoices}"
                     SelectedItem="{Binding SelectedInvoice}"
                     BorderThickness="0">
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:InvoiceRowViewModel}">
                        <Grid Margin="4,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Grid.Column="0"
                                       Text="{Binding InvoiceNumber}" FontWeight="SemiBold"/>
                            <TextBlock Grid.Row="0" Grid.Column="1"
                                       Text="{Binding TotalAmount, StringFormat='{}{0:C}'}"
                                       FontWeight="SemiBold"/>
                            <TextBlock Grid.Row="1" Grid.Column="0"
                                       Text="{Binding InvoiceDate, StringFormat='{}{0:dd MMM yyyy}'}"
                                       Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                            <TextBlock Grid.Row="1" Grid.Column="1"
                                       Text="{Binding EntryCount, StringFormat='{}{0} entries'}"
                                       Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>

        <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch"
                      Background="{StaticResource BorderBrush}"/>

        <!-- Invoice detail -->
        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="16" DataContext="{Binding SelectedInvoice}">

                <!-- No selection placeholder -->
                <TextBlock Text="Select an invoice to view details."
                           Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,40">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- Invoice detail content -->
                <StackPanel>
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>

                    <!-- Header -->
                    <TextBlock Text="{Binding InvoiceNumber}"
                               FontSize="18" FontWeight="Bold" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding InvoiceDate, StringFormat='Date: {0:dd MMM yyyy}'}"
                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding TotalAmount, StringFormat='Total: {0:C}'}"
                               FontWeight="SemiBold" FontSize="14" Margin="0,0,0,12"/>

                    <!-- PDF link -->
                    <Button Content="Open PDF"
                            HorizontalAlignment="Left"
                            Style="{StaticResource SecondaryButton}"
                            Padding="10,5" Margin="0,0,0,16"
                            Command="{Binding DataContext.OpenPdfCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CommandParameter="{Binding PdfPath}"
                            Visibility="{Binding HasPdf, Converter={StaticResource BoolToVisibilityConverter}}"/>

                    <!-- Lines -->
                    <TextBlock Text="Invoice Lines" FontWeight="SemiBold" Margin="0,0,0,6"/>
                    <DataGrid ItemsSource="{Binding Lines}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False" CanUserDeleteRows="False"
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal"
                              BorderThickness="1" BorderBrush="{StaticResource BorderBrush}"
                              Margin="0,0,0,16">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Category" Binding="{Binding Description}" Width="*"/>
                            <DataGridTextColumn Header="Hours" Binding="{Binding Hours, StringFormat='{}{0:N2}'}" Width="70"/>
                            <DataGridTextColumn Header="Rate" Binding="{Binding Rate, StringFormat='{}{0:C}'}" Width="80"/>
                            <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat='{}{0:C}'}" Width="90"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Linked entries -->
                    <TextBlock Text="Work Entries" FontWeight="SemiBold" Margin="0,0,0,6"/>
                    <DataGrid ItemsSource="{Binding LinkedEntries}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False" CanUserDeleteRows="False"
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal"
                              BorderThickness="1" BorderBrush="{StaticResource BorderBrush}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat='{}{0:dd MMM yyyy}'}" Width="105"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                            <DataGridTextColumn Header="Hours" Binding="{Binding Hours, StringFormat='{}{0:N1}'}" Width="60"/>
                        </DataGrid.Columns>
                    </DataGrid>

                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>

</UserControl>
