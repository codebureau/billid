<Window x:Class="WorkTracking.UI.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen"
        Width="480" Height="280">

    <Border Background="#FF0067C0"
            CornerRadius="12"
            MouseLeftButtonDown="OnBorderClick">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Close button (visible only in About mode) -->
            <Button x:Name="CloseButton"
                    Grid.Row="0"
                    Content="✕"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="0,10,12,0"
                    Width="28" Height="28"
                    FontSize="14"
                    Foreground="White"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    Visibility="Collapsed"
                    Click="OnClick"/>

            <!-- Logo -->
            <Image Grid.Row="0"
                   Source="/billid-logo.png"
                   Width="320"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Margin="0,40,0,20"
                   RenderOptions.BitmapScalingMode="HighQuality"/>

            <!-- Footer: version + GitHub -->
            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,0,0,24"
                        Opacity="0.75">
                <TextBlock x:Name="VersionText"
                           Foreground="White"
                           FontSize="12"/>
                <TextBlock Text="  ·  github.com/codebureau/billid"
                           Foreground="White"
                           FontSize="12"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
